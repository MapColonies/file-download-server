{{- if .Values.enabled -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "file-downloader-server.fullname" . }}-configmap
  labels:
    app: {{ template "file-downloader-server.name" . }}
    component: file-downloader-server-configmap
    environment: {{ .Values.environment }}
    release: {{ .Release.Name }}
data:
  NGINX_DOWNLOADS_CONFIG: {{ .Files.Get "config/downloads.conf" | quote }}
{{- end }}
